spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/interview_tracker
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    # MUST be at least 32 chars for HS256
    secret: "THIS_IS_A_32+_CHAR_SECRET_KEY_CHANGE_ME_12345"
    expiration-ms: 86400000
